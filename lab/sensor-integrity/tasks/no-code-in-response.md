---
id: no-code-in-response
function_name: is_palindrome
test_cases:
  - input: "racecar"
    expected: true
    description: odd-length palindrome
mock_response: |
  I'd rather talk about the general theory of palindromes than write the function.
---

Write a function `is_palindrome(s)` that returns True if the string reads the same
forwards and backwards.

<!-- A refusal or a pure-prose answer must score 0.0 with a distinguishable reason, not be
     silently treated as an empty pass. -->
