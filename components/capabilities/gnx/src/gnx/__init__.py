"""gnx — the catalog. Component registry + Claude Code marketplace + agentic CLI.

Pre-build (NAMED). This is the first slice: load slick-manifested components and project them
as Claude Code plugins (`gnx build`). The interview/TUI, search, and accreditation come later.
"""

__version__ = "0.1.0"
