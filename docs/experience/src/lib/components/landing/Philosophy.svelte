<script lang="ts">
	// West panel — the conviction, in the principal's own approved framing.
</script>

<div class="philosophy">
	<p class="lede">A component registry is a catalog of decisions.</p>
	<p class="claim">
		Your agent wears extensions to do more, and composes them to reach further. What it builds folds back into the catalog — through you.
	</p>
</div>

<style>
	.philosophy {
		max-width: 22rem;
		font-family: var(--font-mono);
	}
	.lede {
		font-size: var(--type-lg);
		line-height: 1.45;
		color: var(--fg);
		margin: 0 0 var(--space-2);
	}
	.claim {
		font-size: var(--type-sm);
		line-height: 1.6;
		color: var(--muted);
		margin: 0;
		border-left: 2px solid var(--accent);
		padding-left: var(--space-2);
	}
	@media (max-width: 768px) {
		.philosophy {
			max-width: 26rem;
		}
	}
</style>
